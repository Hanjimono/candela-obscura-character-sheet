import { CharacterData } from "./types/character"

export const DRIVES_TYPES = ["nerve", "cunning", "intuition"] as const

export const ACTION_TYPES = [
  "move",
  "strike",
  "control",
  "sway",
  "read",
  "hide",
  "survey",
  "focus",
  "sense"
] as const

export const MARKS_TYPES = ["body", "mind", "bleed"] as const
